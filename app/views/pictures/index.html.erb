<ul class="small-block-grid-2 medium-block-grid-3 large-block-grid-4">
  <% @pictures.each do |picture| %>
    <li><%= link_to image_tag(picture.photo.url(:medium)), picture_path(picture) %></li>
  <% end %>
</ul>
